<div class="container">
{% for article in articles %}
    <h2>{{ article.title }}</h2>
    <p>{{ article.content }}</p>
    <p>Author: {{ article.author.username }}</p>
    <p>Category: {{ article.category.name }}</p>
    <p>Created at: {{ article.createdAt|date('F j, Y') }}</p>
{% endfor %}
</div>